---
layout: null
permalink: /feed.xml
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site.title | xml_escape }}</title>
    <subtitle>{{ site.description | xml_escape }}</subtitle>
    <id>{{ site.url }}/</id>
    <link href="{{ site.url }}/feed.xml" rel="self" type="application/atom+xml"/>
    <link href="{{ site.url }}/" rel="alternate" type="text/html"/>
    {% if site.posts.size > 0 %}<updated>{{ site.posts.first.date | date_to_xmlschema }}</updated>{% else %}<updated>{{ site.time | date_to_xmlschema }}</updated>{% endif %}
    <author>
        <name>{{ site.author | xml_escape }}</name>
        <uri>{{ site.url }}/</uri>
    </author>
    <rights>© {{ site.time | date: "%Y" }} {{ site.author | xml_escape }}</rights>

    {% for post in site.posts limit: 30 %}
    <entry>
        <title type="text">{{ post.title | xml_escape }}</title>
        <id>{{ site.url }}{{ post.url }}</id>
        <link href="{{ site.url }}{{ post.url }}" rel="alternate" type="text/html"/>
        <published>{{ post.date | date_to_xmlschema }}</published>
        <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
        <author>
            <name>{{ site.author | xml_escape }}</name>
        </author>
        {% for tag in post.tags %}<category term="{{ tag | xml_escape }}"/>
        {% endfor %}
        <summary type="text">{{ post.description | default: post.excerpt | strip_html | normalize_whitespace | truncate: 300 | xml_escape }}</summary>
        <content type="html">{{ post.content | xml_escape }}</content>
    </entry>
    {% endfor %}
</feed>
